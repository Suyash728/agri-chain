# requirements.txt
# -----------------
# Python dependencies for the AgriChain AI Trust Layer.
#
# Only the packages actually used in the current phase are listed here.
# Additional packages (scikit-learn, web3, etc.) will be added in
# later phases as each pipeline stage is implemented.
#
# NOTE: >= bounds used so pip picks releases with pre-built binary
# wheels for your Python version (avoids needing MSVC to compile Rust).

# FastAPI: Web framework for building the REST API.
fastapi>=0.115.0

# Uvicorn: ASGI server used to run the FastAPI application.
# The [standard] extra includes watchfiles, httptools, websockets.
uvicorn[standard]>=0.30.6

# Pydantic: Data validation library (FastAPI dependency).
pydantic>=2.9.2

# ---------------------------------------------------------------------------
# Machine Learning dependencies (Phase 5+)
# ---------------------------------------------------------------------------

# scikit-learn: Unsupervised Isolation Forest anomaly detection.
scikit-learn>=1.5.0

# ---------------------------------------------------------------------------
# Testing dependencies (Phase 2+)
# ---------------------------------------------------------------------------

# pytest: Test runner.
pytest>=8.0.0

httpx>=0.27.0

# matplotlib: Generation of evaluation benchmark charts and confusion matrices.
matplotlib>=3.8.0

